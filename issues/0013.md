# 0013 — Ad hoc custom effects: nine effect modes, arbitrary colors, and a bounded auto-revert timeout

| | |
|---|---|
| **Status** | open |
| **Module** | LED / Rendering / MQTT |
| **Platform** | ESP32-C3 |
| **First seen** | 2026-08-17 |

## Description

Colors and effects can only be chosen from six hardcoded themes. Implement the engine in `docs/glowkitchen-custom-effects-spec.md`: built-in themes become named presets of a generic renderer, arbitrary colors and effects arrive over MQTT, and a custom effect can carry an **optional timeout after which the strip reverts to its previous theme on its own** — up to a maximum of 8 hours, so an effect can be set for an event and left to run.

**This is an umbrella ticket** — it scopes the work and ships no code itself. The four phases below are the shippable pieces; the shared design they all follow is in `## Long Description`.

## Children

Four phases, in dependency order. Each is independently shippable and separately verifiable; each carries its own plan, branch, and review.

| # | Phase | Scope | Status |
|---|---|---|---|
| [#0014](0014.md) | Engine refactor | `EffectMode` enum, `BUILTIN_EFFECT_MODE[]`, render dispatch, `THEME_CUSTOM` slot | resolved 2026-08-28 — mirror mode and IR remote named as unverified, both need eyes on the strip |
| [#0015](0015.md) | Seven new renderers | Chase, Wipe, Scan, Sparkle, Pulse, Strobe, Colorloop | resolved 2026-08-28 — COLORLOOP carved to [#0018](0018.md), fixed there |
| [#0016](0016.md) | MQTT + persistence | `SET_EFFECT` / `CLEAR_EFFECT`, hand-rolled parser, NVS + `SETTINGS_VERSION` | resolved 2026-08-28 — re-confirmed on hardware |
| [#0017](0017.md) | Timeout + revert | Wall-clock end time, reboot semantics, `STATUS timeoutRemaining` | resolved 2026-08-28 — timeout re-measured at 29.991 s / 15.002 s |

Keep this table current as each child moves. This umbrella stays `in-progress` until all four are terminal, and only then becomes a candidate for `resolved`.

### Acceptance criteria for the whole

- Any color set and any of the nine effects can be applied over MQTT, optionally for a bounded period that reverts on its own.
- The six built-in themes, the IR remote, and every existing MQTT command behave exactly as before.
- `firmware.bin` still fits the OTA slot with meaningful headroom — see [#0009](0009.md) for the cost of getting this wrong.
- No renderer or timeout check uses a `deadline < now` comparison — see [#0008](0008.md).
- `docs/mqtt_commands.md`, `docs/add_new_theme.md`, and the `README.md` feature list all reflect the shipped behavior.

## Long Description

### The nine effect modes

| # | mode | new? | behavior |
|---|---|---|---|
| 0 | `BLEND` | existing | today's `slowBlend()` — gradient blending, scrolling |
| 1 | `FLICKER` | existing | today's `flickerLEDs()` — per-LED candle flicker |
| 2 | `CHASE` | new | a run of lit LEDs travelling along the strip |
| 3 | `WIPE` | new | color fills progressively from one end |
| 4 | `SCAN` | new | Larson/Cylon — a band sweeping back and forth |
| 5 | `SPARKLE` | new | random LEDs pop and fade |
| 6 | `PULSE` | new | breathing — whole strip fades up and down |
| 7 | `STROBE` | new | hard on/off flashing |
| 8 | `COLORLOOP` | new | whole strip cycles through the colors |

`speed` and `intensity` (0–255, default 128) modulate each; `intensity` is per-effect — flicker depth, chase width, sparkle density.

All six built-in themes become `(EffectMode, color array)` pairs — Green → `FLICKER`, the other five → `BLEND` — rendering identically while going through the new engine.

### Timeout and revert

A custom effect may carry an optional `timeout` in seconds:

- **omitted or `0`** — the effect stays until explicitly changed
- **1 … 28800** — after that many seconds the device reverts to the theme that was active before the effect was applied
- **above 28800 (8 hours)** — clamped to 28800, not rejected

8 hours covers an evening event without a strip being left in strobe indefinitely because someone forgot. Both ends of the range matter: 30 seconds for a quick look, 20 minutes for dinner, 8 hours for a party.

**Clamp rather than reject.** A request for 12 hours is a clear intent to "leave it on for the event", and refusing the whole command over it produces nothing at all. The clamp must be visible though — `STATUS` reports `timeoutRemaining`, so an unexpectedly short value is discoverable rather than silent.

`CLEAR_EFFECT` reverts immediately without waiting for the timeout:

```
lights/kitchen/cmd  →  CLEAR_EFFECT
```

Unlike `OTA_URL` and `RESTART`, this is safe on `lights/all/cmd` — the worst case is the whole installation returning to its normal theme, which is the recovery action anyway.

An explicit theme command (`RAINBOW`, `GREEN`, …) must also cancel an active custom effect rather than leaving it queued to revert later. Reverting to a theme the user has since moved away from would be baffling.

### Surviving a reboot — the part the spec does not settle

`activatedAt = millis()` resets to zero on boot, so a persisted effect would silently restart its timer. For an 8-hour event effect interrupted by a power blip at hour 7, that means another 8 hours.

Neither extreme is right: always reverting on boot loses a long-running event effect to a momentary glitch; always resuming restarts short ones that should have expired during the reboot itself.

Persist an **absolute wall-clock end time** instead of an elapsed offset. NTP is already configured for the OTA scheduler, so on boot:

- clock valid and `now < endTime` → resume the effect with the correct remaining time
- clock valid and `now >= endTime` → revert immediately, the effect expired while powered down
- clock not yet synced → revert; a wrong guess about a timed effect is worse than dropping it

This makes a 30-second effect expire across a reboot and an 8-hour one resume with the right remainder, which is what both cases want.

### Palette representation — CHSV triples, decided 2026-08-26

`SET_EFFECT` accepts `#RRGGBB`, but the engine as built in [#0014](0014.md) stores **one hue byte per color** and renders `CHSV(hue, 255, 200)` — saturation and value are fixed. White, grays, and pastels are therefore inexpressible: `#FFFFFF` has no hue and would render as an arbitrary saturated color. [#0015](0015.md)'s and [#0016](0016.md)'s plans both hit this from opposite sides.

**Decision: widen the palette to CHSV triples.**

```c
// was — hue only
uint8_t colors[8];
CHSV(colors[i], 255, 200)

// now
struct PaletteColor { uint8_t h, s, v; };
PaletteColor colors[8];   // 24 bytes
CHSV(c.h, c.s, c.v)
```

Rationale, and why not the alternatives:

- **BLEND's hue-circle interpolation is preserved**, because blending still runs on the `h` component alone. This is what ruled out a flat `CRGB` palette in [#0014](0014.md)'s plan §9, and that reasoning still stands — this change routes around it rather than reopening it.
- **The six built-in themes become `{hue, 255, 200}` triples and must remain byte-identical** through the frame-capture harness. That is the acceptance test for this change: if `test/test_effects/` goes red, the widening is wrong.
- Cost is 16 extra bytes of NVS per effect and a wider `PaletteColor` in `EffectState`. Negligible against the 83,744-byte headroom.
- Rejected: documenting the limit (silently renders the wrong color for any desaturated input) and rejecting unrepresentable colors (a user picking from a color wheel gets refusals with no discoverable cause).

**Ownership:** [#0015](0015.md) performs the widening, since it owns the renderer signatures and already touches `src/effects.h` and `src/themes.h`. [#0016](0016.md) then parses hex straight into `PaletteColor` via RGB→HSV, with no lossy hue-only step.

### Timing must use the rollover-safe helper

Every new renderer and the timeout check must go through `timeReached()`, not a `deadline < now` comparison. Seven of those were removed in [#0008](0008.md) after they froze four strips for days; nine new renderers are nine new chances to reintroduce the pattern. 8 hours is well inside the ~24.8-day window `timeReached()` handles.

The spec's own timeout example already uses the correct elapsed form — worth keeping, and worth checking that `EVERY_N_MILLISECONDS` (a FastLED macro that wraps `millis()` internally) is not substituted for it without thought.

### ArduinoJson will not fit

The spec says to pull in ArduinoJson and check headroom. **Current headroom is 84,000 bytes** and ArduinoJson typically costs 20–30 KB before a single renderer is written. Given [#0009](0009.md) — a release published that no device could install, for exactly this reason — the parse should be hand-rolled. The payload is a fixed shape with at most 8 hex strings, so a parser is far cheaper than a general-purpose one.

If ArduinoJson is used anyway, settle the `min_spiffs` partition question ([#0009](0009.md) notes, [#0004](0004.md) item 6) **first** — that decision needs a USB pass on every device and cannot be discovered late.

### The compatibility promise is currently untestable

The spec guarantees the six built-in themes produce the same visual output through the new engine. Nothing today can verify that; the only available check is looking at a strip. Decide up front whether "identical" means capturing `leds[]` after a fixed number of frames and diffing it against the old renderer, or whether visual inspection is accepted. A promise that cannot be checked is not a guarantee.

### From the spec, to keep as written

- **NVS versioning** — a `SETTINGS_VERSION` byte, with `customEffect` zero-initialized on mismatch rather than trusting whatever bytes occupy the newly-used region after an OTA.
- **`THEME_CUSTOM` guard** — booting with `currentTheme == THEME_CUSTOM` but an uninitialized effect falls back to `THEME_GREEN` rather than rendering an empty color array.
- **Malformed input** — validate `mode` against the enum and `colors` count (1–8), and on any parse failure keep the prior state rather than applying a partial config.
- **`NEXT_THEME` / `PREV_THEME` cycle only the original six**, so `THEME_CUSTOM` is unreachable by cycling.

## Expected behavior

Any color set and effect can be applied over MQTT, optionally for a bounded period after which the strip returns to its previous theme by itself. `CLEAR_EFFECT` returns it immediately. The six built-in themes, the IR remote, and every existing MQTT command behave exactly as before.

## Actual behavior

Only six hardcoded themes exist. There is no way to set arbitrary colors, no effects beyond blend and flicker, and no way to apply a temporary look that reverts on its own.

## Notes

- Filed as one issue, split into an umbrella on 2026-08-25 — it has four independently-shippable pieces, past the ~3 threshold. See `## Children`.
- Docs to update on landing, each in the child that introduces the behavior: `docs/mqtt_commands.md` in [#0016](0016.md) (`SET_EFFECT`, `CLEAR_EFFECT`, and the note that prior commands are unaffected) and [#0017](0017.md) (`timeoutRemaining`); `docs/add_new_theme.md` in [#0015](0015.md) (themes are now `(EffectMode, color array)` pairs). The `README.md` feature list is updated on this umbrella when the last child lands.
- ~~[#0007](0007.md) line 49 has a broken cross-reference.~~ Already corrected — that line now points at [#0013](0013.md). Verified 2026-08-25.
- Scheduled seasonal palettes ([#0007](0007.md)) become possible once this lands: a date-based cron entry on the Pi publishing `SET_EFFECT`, with no device-side calendar.


## Handoff — 2026-08-26, unresolved

**Read this before touching anything.** The engine is built, merged and works. The demo does not, and the reason is external to this repo.

### The blocker

`scripts/demo_effect.sh <device> <preset>` publishes correctly, the device accepts and applies the effect (`SET_EFFECT applied: …` in the serial log, ~0.17 s after publish), and then **something else on the broker overrides it within seconds**. A 45-second sniff of `lights/#` while publishing nothing showed:

```
lights/tv/cmd         SET_BRIGHTNESS:113    (retained, not ours)
lights/desk/cmd       SET_BRIGHTNESS:113
lights/kitchen/cmd    SET_BRIGHTNESS:113
lights/workbench/cmd  SET_BRIGHTNESS:113
lights/recycling/cmd  SET_BRIGHTNESS:113
lights/all/cmd        STATUS                 (live, periodic)
```

A Home Assistant automation is actively managing these lights. The dev board subscribes to `lights/all/cmd` like every other strip, so anything broadcast there overrides whatever was just set. There was also a **retained `FOREST` on `lights/all/cmd`** which is redelivered on every reconnect and snaps the board back to Forest.

### What to do first, before any debugging

1. Clear the retained broadcast and **leave it cleared**:
   `mosquitto_pub -h $MQTT_BROKER -u $MQTT_USERNAME -P $MQTT_PASSWORD -t lights/all/cmd -r -n`
2. Disable the Home Assistant automation publishing to `lights/all/cmd`. This is in HA, not in this repo, and needs the user's decision.
3. Only then test `scripts/demo_effect.sh 84fce68774a4 dolly`.

**Only the connected dev board matters.** The other five strips run 0.0.5 and do not have this firmware. Do not spend effort protecting their retained state — restoring that retained `FOREST` is what repeatedly broke the demo during this session.

### Process failure worth not repeating

Verification ran through the serial log, which proves the device *accepted* a command. It does not prove anything about what is on the strip a second later, because that depends on whatever publishes next. Roughly an hour went into re-confirming "the firmware works" while the actual cause sat unexamined on the broker. **When a user's observation contradicts the logs, sniff the whole topic tree before anything else** — it is a 45-second test. Also: only change one variable at a time. Clearing/restoring the retained message, reflashing, and editing presets were interleaved, which made observations non-comparable.

### State of the work

All four phases merged to `main`; 87 native tests; release binary 1,235,984 with 74,736 spare. Nothing has been released — no GitHub release, no OTA. The dev board carries a USB `esp32c3-debug` build with `EFFECT_TRACE`.

Confirmed on hardware: theme dispatch and the six-theme cycle; all seven renderers rendering; `SET_EFFECT`/`CLEAR_EFFECT`; NVS persistence across reboot; timeout firing at exactly the requested second; reboot-resume reporting "92 s remaining" of a 120 s effect.

Not confirmed: that the demo presets look right to a person, because the override above prevented any clean look.

### Tools built this session

- `scripts/demo_effect.sh` — nine presets, `--speed` / `--intensity` overrides, `stop`.
- `scripts/trace_effect.sh` — reads `EFFECT_TRACE` telemetry and reports whether the strip is animating, phase movement, lit-LED count, and the real saturation/value of each colour written. **Use this instead of asking the user to look.**
- `scripts/demo_timed.sh` — reports publish-to-apply latency.

### Open follow-ups

- [#0018](0018.md) — COLORLOOP crawls (±1 hue per tick).
- [#0019](0019.md) — colour conversion fixed and merged; unconfirmed by eye.
- `e6e3718` (SCAN band width) landed mid-review and never had an independent review pass.
- `SET_BRIGHTNESS:225` came back as 113 — unexplained, possibly the same external publisher, possibly a second bug.
- Effects render at ~50% of the brightness setting via `hsv2rgb_rainbow` attenuation. Unfiled.

## Relation

- Children: [#0014](0014.md), [#0015](0015.md), [#0016](0016.md), [#0017](0017.md) — the four phases; see `## Children`.
- Follow-ons found while verifying on hardware: [#0018](0018.md) (COLORLOOP crawls), [#0019](0019.md) (colour conversion undersaturated custom palettes).
- Unblocks: [#0007](0007.md) — seasonal colors become scheduled `SET_EFFECT` commands instead of built-in themes.
- Constrained by: [#0009](0009.md) and [#0004](0004.md) item 6 — flash headroom decides whether a JSON library is affordable.
- Must not regress: [#0008](0008.md) — nine new renderers must use `timeReached()`, not deadline comparisons.
