#ifndef CONFIG_H
#define CONFIG_H

#include <Arduino.h>
#include <WiFi.h>

// These are static so each file that includes Config.h gets its own copy.
// For constants, this is generally fine on ESP32.
static const char* WIFI_SSID = "YOUR_WIFI_SSID";
static const char* WIFI_PASS = "YOUR_WIFI_PASSWORD";

static const char* MQTT_HOST = "YOUR_MQTT_BROKER_IP";
static const uint16_t MQTT_PORT = 1883;
static const char* MQTT_USER = "mqtt";
static const char* MQTT_PASS = "YOUR_MQTT_PASSWORD";

// For non-constants, we use extern and define them in Config.cpp
// but we keep the initial values here via macros so they aren't in source control.
#define INITIAL_DEVICE_NAME ""
#define INITIAL_IR_ENABLED false

extern String DEVICE_NAME;
extern bool irEnabled;

// Function declarations
String deviceIdFromMac();
String deviceIdFromMacCompact();
String getDeviceId();
String getDeviceName();

#endif // CONFIG_H
